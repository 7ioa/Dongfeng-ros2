# 自主巡航实施记录

计划：2026-09-21-autonomous-perimeter.md。用户已批准设计并多次要求继续完成。

- 当前分支 feat/autonomous-perimeter；在用户原目录实施，不推送远程。
- 已接通 RGB 相机、CameraInfo、激光、IMU。VMware 的 EGL 路径会强制 SVGA3D，产生空白帧；已实测 X11 + LIBGL_ALWAYS_SOFTWARE=1 + Ogre2 输出正常相机和 720 条有效扫描。
- 已实现路线核心、视觉道路/灯色、动态 SDF 灯泡、交通灯服务节点、人工/自动仲裁及启动入口。
- 30 项回归通过（autonomy 16、bringup 14），随后新增定位和源时戳 4 项也通过。
- 独立审查提出的无效扫描当作畅通、时钟回退后恢复、空闲键盘覆盖自动权限已修复并有回归。
- 实际首弯暴露轮式里程计在上下坡及滑移转向时漂移：run4 真值停在 y≈0.91，轮式预测却前进到灯前。当前不能声称整圈完成。
- 决策：在既有地图上做激光地标匹配（不是 SLAM，也不读取真值）；从静态视觉网格采样竖直表面，IMU 提供姿态，点到面匹配修正 x/y。地标文件 landmarks.npz 已生成，测试通过，现已接入节点。
- 当前正在进行带扫描定位和 IMU 转向反馈的整圈实测。待办：动态灯相机实测、整圈轨迹/边界验收、改变相位重跑、故障注入、文档和最终审查。

- run6 通过首弯和右侧红绿灯，后坡转弯卡住。短时测得 0.2 rad/s 指令实际约 0.01。
- WheelSlip 首次使用 fdir1=0 0 1 后变差；运行时三组对照：零滑移 yaw≈0.162、第一方向滑移 yaw≈-0.007、第二方向滑移 yaw≈0.167。结合 DART 接口确认轮链接 +Y 才是横向轴；改为 fdir1=0 1 0，保留 lateral=.5、longitudinal=0。

## 2026-09-21 暂停交接

用户因额度要求停止。完整接续文档：`docs/自主巡航开发进度与接续指南.md`。run8 已结束，未跑通（进度约 0.925 m，首坡卡住）。此前对 fdir1 的方向判断尚未被运行验证，请勿视为已解决。最新审查另发现提前 committed、前方扫描盲区、验收假阳性三个未修复问题，详见交接文档。暂停时未见任务仿真进程，代码未提交。


## 2026-09-22 接续

- 已读取接续指南、批准设计和计划。继续当前 feat/autonomous-perimeter 工作区，保留所有旧修改；不重新建工作树，不提交或推送。
- Ruling: GUI 使用 QT_QPA_PLATFORM=xcb、--render-engine ogre；服务器传感器保留独立 Ogre2 参数，以实际图像/扫描验证兼容性。
- Ruling: stop_s 表示车体中心停车目标；line_s 表示实体停车线，车头前伸 0.1032 m，停车目标在车头距线 0.035 m 处。只有实际车头越线才锁存 CROSSING。
- 待办：三项安全/验收回归 → 受控动力学诊断 → 真实整圈和变相位 → 故障注入 → README/报告/审查。


## 2026-09-22 再次继续与显示修复

- 用户短暂要求暂停整理，随后明确恢复开发，并要求后续采用 launch_car.sh 的场景加载方式。
- 自主入口现在直接调用 launch_car.sh --autonomy；场景窗口 X11/Ogre1、无强制软件渲染。仅传感器服务器使用 Mesa/Ogre2。已检查实际进程环境，用户确认画质/流畅度恢复或明显改善。
- run10 实际通过 B/C/D/A，13.704/14.764 m 后在 A 弯下坡因定位发散停车；两处绿灯通过、无红灯等待，总验收失败。
- 根因：局部 ICP 在约 -2.7 cm 的 Y 偏差下收敛到重复表面；真实扫描重放累积约 -24 cm X 偏移。新增 ±6 cm 粗搜索+Cauchy 权重后回归通过。真实扫描夹具在 test/fixtures/a_bend_scan.npz，采样在 reports/autonomy/probe_localization_a。
- 独立审查四项 Important 已分别复现并修复：手动越线后倒车撤销 commitment、转弯车尾扫掠、IMU 四元数/缺失姿态验证及旧帧拒收、验收轮廓从四角扩展至完整边缘。
- 最新自主回归 49 项通过（36.14 秒），bringup 17 项通过。审查复核进行中。
- run11：通过 launch_car.sh --autonomy、带 GUI、phase13、domain93 进行全圈；已实测右侧红灯停车并绿灯恢复。尚待最终 summary，不提前标记完成。
- 待办：run11 结果 → 变相位重跑 → validate_faults 实跑（已编写尚未验证）→ 最终 README/指南/验收报告。


## 2026-09-22 最后暂停交接（覆盖上述“运行中”状态）

用户明确要求停止开发并总结，当前无任务仿真/测试残留进程。完整现状、证据与下一步见 `docs/自主巡航开发进度与接续指南.md`，已重写。

- run11 实际完成 B/C/D/A 并回到起点，但停车仅记录 1.943 s，最终 lap_pass=false。已修正验收采集时长，未重跑。
- 用户确认 launch_car.sh 渲染画质/流畅度改善；新反馈灯色视觉异常仍待调查，不能认定已修复。
- 左右固定红/绿/黄切换 GUI 实际材质与相机共 8 项通过，证据 signal_baseline；尚未做窗口卡顿复现或整圈 GUI 灯色采集。
- 速度已从 .10/.05 提至 .13/.07 m/s，尚未物理整圈验证。
- 之前声称手动倒车修复完整并不准确：审查另发现长倒车投影窗口问题。本轮追加 LongManualReverseTest 并修复，最新自主 50 项通过；需审查复核和实际异常注入。
- 当前 HEAD 为 1f7a4f5，保留未提交修改和未跟踪探针/报告；未推送/合并。
